# 项目案例：机械部智能自采系统

> 面向机械部工程师的**采购需求提报与全流程跟踪系统**。用「对话式填报 + LLM 结构化抽取」把工程师口语化、碎片化的采购需求，自动转成规范、可校验、可追踪的采购单，并打通提效数据看板。
>
> 一句话价值：**把采购的非结构化入口变成结构化入口**——工程师从"发消息、反复澄清"变成"一句话/一张截图搞定"，采购员从"手动整理"变成"接收干净数据"。

---

## 1. 背景与问题

机械部工程师的采购需求原本散落在飞书消息、口头、截图里，采购员要手动整理、反复澄清才能下单。问题本质是**采购流程的入口是非结构化的**——下游的跟踪、统计、比价、预算全都受制于这个脏入口。

目标：做一个**低门槛的结构化入口**，让工程师零学习成本地提报，系统自动抽取、校验、生成采购单，并沉淀出可分析的数据。

## 2. 我的角色与独立度

独立负责**数据建模（6 表结构/索引/关系）+ 后端业务逻辑 + 对话式填报的 AI 抽取管线 + 提效看板**。平台（飞书妙搭 apaas）提供基础设施：托管 PostgreSQL、鉴权、能力网关（LLM/飞书）。AI 抽取的提示词、规则/LLM 混合策略、批量与多规格逻辑、权限模型均由我设计并迭代。

## 3. 技术栈（含选型理由）

| 层 | 技术 | 为什么选它 |
|---|---|---|
| 后端 | NestJS 10 + TypeScript | 模块化、依赖注入，工程结构清晰 |
| ORM / DB | drizzle-orm + PostgreSQL | schema 即 TS 代码、类型安全、SQL 透明；PG 原生支持 jsonb/timestamptz/数组/RLS |
| 前端 | React + Vite + TypeScript | 快构建、类型安全 |
| UI | Tailwind + shadcn/ui | 一致设计系统、可组合 |
| 状态 | TanStack Query | 服务端状态缓存/失效/乐观更新，替代手撸 useEffect |
| AI | 豆包 Doubao-Seed-1.6（文本结构化抽取 + 视觉截图识别） | 结构化 JSON 输出；规则+LLM 混合，PII/结构字段不进 LLM |

## 4. 系统架构

```
工程师 ──对话/截图──▶ React 前端（对话填报 · 表单填报）
                         │ REST
                         ▼
              NestJS 服务（procurement 提报跟踪 · analytics 看板）
                         │
        ┌────────────────┼─────────────────────┐
        ▼                ▼                     ▼
   PostgreSQL      豆包 LLM（文本/视觉）      飞书（通知 · 多维表格）
   事实源           结构化抽取               采购台账（价格/工时回流）
```

分层：前端（对话/表单双模式）→ NestJS 模块 → drizzle → PostgreSQL；旁挂三个能力：LLM 抽取、飞书通知、多维表格双向同步。

## 5. 数据模型（6 张表 · 双源设计）

| 表 | 作用 | 关键字段 |
|---|---|---|
| `procurement_requirement` | **核心·采购需求单**（一物料一行） | requirement_id、item_name、item_brand_model(多规格合并串)、item_link、item_quantity、project_code、status、requester/assignee、draft_started_at(提效埋点) |
| `procurement_status_log` | 状态流转**审计日志** | requirement_id、operator、old/new_status、extra_info(jsonb) |
| `project_info` | 项目主数据（自动补全） | project_code(PK)、project_name、department |
| `purchase_record` | **同步表**·采购台账（飞书多维表格同步） | price、material_category、platform、buyer_minutes(采购员耗时) |
| `config_baseline` | 提效基线参数 | manual_cycle_hours、buyer_hourly_cost、dept_headcount |
| `visitor_record` | 访客记录（附属） | visitor_name、visit_time |

**设计要点**：交易数据（提报+状态）在自建库，外部采购台账从飞书多维表格同步，两条数据线靠 `project_code` 关联，配合基线表算提效 ROI——这是一个**双源 + 分析建模**的库，而非纯 CRUD。

## 6. AI 抽取管线（核心亮点）

抽取不是"调一次 LLM"，而是一条分层管线。核心是 **schema 驱动的结构化信息抽取**：一份 JSON Schema 同时约束三处——约束 LLM 输出、运行时校验、生成静态类型；归一化规则放在 system prompt 里，与 schema 解耦。

```
用户输入/截图
   ├─▶ 规则预抽取（链接·电话·项目号）──┐
   └─▶ LLM 语义抽取（名称·规格·用途）──┴─▶ 置信度合并（规则优先）─▶ 8 字段结构化采购单
```

| 层 | 内容 | 领域 |
|---|---|---|
| **L0 核心引擎** | schema 驱动的结构化抽取，8 字段，归一化规则与 schema 解耦 | Structured Extraction |
| **L1 可靠性** | 规则+LLM **混合路由**（按置信度/成本分流）+ shadow 对比可观测（规则 vs LLM 分歧日志） | Hybrid IE / LLMOps |
| **L2 交互** | 对话式**槽位填充**（缺哪个问哪个）+ **上下文工程**（喂"已收集+当前询问+近期对话"的带状态 payload） | Task-Oriented Dialogue |
| **L3 覆盖度** | 多模态截图识别（份数换算 ⌈总量÷每份⌉）+ 批量分解（多物料切段并行 + 多规格合并求和） | Multimodal IE |
| **L4 演进** | 物料归一/推荐（候选召回 + LLM 精判）→ Agent 接 PDM 自动申请 | Entity Linking / Agent |

## 7. 权限模型（纵深防御）

- **应用层业务鉴权**：服务层做细粒度授权——归属校验（仅提报人可改）+ 状态机约束（仅"待采购"状态可改）。
- **数据库层 RLS**：PostgreSQL 行级安全，请求注入会话变量 `app.user_id`，策略控制工作区级读写，作为兜底。

粗粒度隔离交给 DB RLS，细粒度业务规则在应用层——即使应用层漏判，DB 层也拦得住越权。

## 8. 工程难点与取舍

**① 让非专业用户口语化提需求，自动结构化**
- 解：规则 + LLM **并行**——正则抓结构不变的字段（电话/链接/项目号，零成本、确定性），LLM 补语义字段；字段级状态机逐项追问补全。
- 取舍：不纯 LLM——电话/链接这类结构字段抽错不可接受，且正则更快更省；不纯规则——语义字段规则搞不定。
- 可靠性：记录规则 vs LLM 的 **shadow 分歧日志**，作为评测集的种子；"数据变了怎么办"靠度量闭环迭代，而非拍脑袋调 prompt。

**② 一句话多物料 + 同物料多规格**
- 解：规则按"链接/买"边界**切分**（稳定），每段**并行调 LLM**；同物料多规格从原文按档解析、份数求和，合并存 `型号A(40份)、型号B(50份)`。
- 取舍：合并存字符串而非拆规格子表——下游台账同步和需求跟踪的粒度都是按物料整条走的，拆子表徒增复杂度；一物料一行而非"订单头+明细"——需求以物料为独立跟踪/指派单位。

**③ 双写一致性（自建库 ↔ 飞书多维表格）**
- PG 是**唯一事实源**，多维表格是下游投影 + 采购员作业台。同步是 **best-effort + 重试**，失败只记日志、不阻断提单（可用性 > 下游实时一致），一致性模型是**最终一致**。
- 改进方向：生产级上 **outbox 模式**（同步任务落表、后台重放）或对账任务补齐差异。

**④ PII 与第三方 LLM**
- 电话/地址这类结构化 PII 本就由正则处理，**不需要进 LLM**；更合规的做法是发 LLM 前脱敏占位——与"PII 归规则、语义归 LLM"的混合架构自洽。

**⑤ 并发与幂等**
- 编号"当天序号+1"有 TOCTOU 竞态，靠唯一索引兜底；生产级应换数据库序列 / `INSERT ... ON CONFLICT`，并把"插入需求 + 状态日志"包进事务、加请求级幂等键。

## 9. 成果

- 覆盖：机械部 **51 名工程师**全量使用；
- 提效：单个需求澄清**沟通轮次 6.5 → 0.2**；
- ROI：采购员协调工时**月省 ≈40h**。

> *指标为试点期间抽样估算；精确化方案为 `draft_started_at` + `buyer_minutes` 全量埋点。*

## 10. 演进规划

- **物料归一 / 智能推荐**：抽取结果匹配标准物料主数据（候选召回 + LLM 精判重排）。
- **接入 PDM**：以 Agent（工具调用）自动完成"匹配物料 → 填表 → 提交申请"，人在环确认。
- **抽取评测流水线**：把 shadow 对比日志沉淀为评测集，按字段测准确率、持续迭代。
- **多租户化**：从部门工具升级为全公司采购需求智能入口/中台（多部门/品类/审批流、对接 ERP/供应商、容器化横向扩展）。

---

## 附：技术关键词索引（面试速查）

**AI**：结构化信息抽取 · Structured Output · 槽位填充(Slot Filling) · 任务型对话(TOD) · 上下文工程 · 规则+LLM 混合路由 · 多模态 IE · shadow 对比/评测 · 实体链接 · Agent 工具调用
**后端/数据**：NestJS · drizzle · PostgreSQL · RLS 行级安全 · 双源数据建模 · 审计日志 · 最终一致/outbox · 幂等/事务
**前端**：React · TanStack Query · 对话/表单双模式 · sessionStorage 会话持久化

*（本文档为项目案例说明，不含任何代码或凭据；涉及的人名、内部标识已脱敏。）*
